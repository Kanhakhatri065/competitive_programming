#include <bits/stdc++.h>

using namespace std;

int main() {
    int testcases;
    cin >> testcases;

    int size,m;
    while(testcases--) {
        cin >> size >> m;

        vector<int> v(size, 0);
        for(int i = 0;i < size;i++) {
            cin >> v[i];
        }

        

    }

    return 0;
}