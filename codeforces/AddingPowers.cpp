#include <bits/stdc++.h>

using namespace std;

int main() {
    int testcases;
    cin >> testcases;

    int n, k;
    for(int i = 0;i < testcases;i++) {
        cin >> n >> k;

        long long int v[n];
        for(int j = 0;j < n;j++) {
            v[j] = 0;
        }

        long long int a[n];
        for(int j = 0;j < n;j++) {
            cin >> a[j];
        }

    }

    return 0;
}